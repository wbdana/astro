import React from 'react'

class Asteroid extends React.Component {
  // 8 sides
  //   Random length, random angle (off of vertical, where this angle is greater than the prior angle)
  //   ctx.closePath()
}

export default Asteroid
